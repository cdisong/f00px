

     {this.props.followers.map((user) => { 
          return (
            <h2>user.username</h2>,
            <img src={user.profile_img_url}/>,
            <button className="follow" onClick={this.createFollow()}></button> ///haveto makeinto boolean later, just check for functionality right now
          );
        })}

          {this.props.following.map((user) => { 
          return (
            <h2>user.username</h2>,
            <img src={user.profile_img_url}/>,
            <button className="unfollow" onClick={this.deleteFollow()}>Unfollow</button> ///haveto makeinto boolean later, just check for functionality right now
          );
        })}